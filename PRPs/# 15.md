
**Role:** Senior Full-Stack Architect (Electron/React)

**Status:**
**CRITICAL DEPENDENCY FAILURE.**
The `JSAmbisonics` library is not loading.

* **Analysis:** The "Script Tag" method failed (likely due to Electron's `file://` protocol blocking relative paths or 404s).
* **Solution:** We will switch to **Direct Source Import**. instead of relying on pre-bundled builds or script tags, we will import the source file directly into the bundle.

**Task:**
Force-integrate `JSAmbisonics` by importing its specific build file, bypassing the broken default entry point.

**1. Install (or Re-Verify) the Package**
Ensure the package is present:

```bash
npm install jsambisonics

```

**2. The "Deep Import" Fix (`src/tools/AmbiRotate/PreviewEngine.ts`)**
The default import (`import * as ...`) often returns `undefined` for older libraries in Vite. You must import the **release file** explicitly.

* **Change your import to this:**
```typescript
// Force Vite to bundle this specific file
import JSAmbisonics from 'jsambisonics/release/jsambisonics.js'; 

```


*(Note: If the linter complains "Could not find declaration file", ignore it or add `@ts-ignore` above the line).*
* **Add this "Sanity Check" immediately after:**
```typescript
console.log("FORCE IMPORT STATUS:", JSAmbisonics);
if (!JSAmbisonics) {
    console.error("FATAL: JSAmbisonics is still null. Bundler failed.");
} else {
    console.log("SUCCESS: JSAmbisonics loaded. Rotator available:", !!JSAmbisonics.SceneRotator);
}

```



**3. Initialize with Safety Checks**
In your `setupAudio()` function, use the imported object safely:

```typescript
if (!JSAmbisonics || !JSAmbisonics.SceneRotator) {
    throw new Error("Library failed to init");
}
const rotator = new JSAmbisonics.SceneRotator(order);

```

**4. Alternative: The "Local Vendor" Fallback (Plan B)**
If the `npm` import above *still* fails (returns undefined):

1. Go to `node_modules/jsambisonics/release/jsambisonics.js`.
2. **COPY** that file.
3. **PASTE** it into `src/vendor/jsambisonics.js`.
4. Change the import to: `import JSAmbisonics from '../../vendor/jsambisonics.js';`
5. *Why:* This forces Vite to treat it as your own source code, not an external package, which often bypasses strict module rules.

**Action:**

1. Try the **Deep Import** fix first.
2. Run the app and check the console for "FORCE IMPORT STATUS".
3. If that fails, execute **Plan B (Local Vendor)** immediately.

