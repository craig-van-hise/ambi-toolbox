

**Role:** Senior Full-Stack Architect (Electron/React)

**Status:**
**EMERGENCY DIAGNOSTIC REQUIRED.**
The `AmbiRotate` preview is **SILENT**. We have tried fixing the file loading, but it still fails. We are flying blind.

**Task:**
Instrument the `AmbiRotate` tool with aggressive debugging and a "Sanity Check" feature.

**1. Implement a "Sanity Check" Tone**

* **Goal:** Verify if the `AudioContext` works *at all*, independent of file loading.
* **Action:** Add a temporary button labeled "🔊 Test Tone" in the UI.
* **Logic:** When clicked:
```javascript
const ctx = new (window.AudioContext || window.webkitAudioContext)();
const osc = ctx.createOscillator();
osc.connect(ctx.destination);
osc.start();
osc.stop(0.5); // Beep for 0.5s
console.log("Test Tone Context State:", ctx.state);

```


* **Why:** If this button doesn't beep, the problem is **Electron/Chromium Audio Permissions**, not your code.

**2. Add a Visual Debug Log (Temporary UI)**

* **Action:** Add a `<pre>` / text box area at the bottom of the AmbiRotate tool.
* **Logic:** redirect all audio-related errors and status updates here so we can see them without opening the DevTools console.
* Print: "AudioContext State: [suspended/running]"
* Print: "File Loaded: [Yes/No]"
* Print: "JSAmbisonics Object: [Defined/Undefined]"



**3. Verify the "JSAmbisonics" Import**

* **Hypothesis:** The library might not be loading correctly via `import`.
* **Action:** In your component, explicitly log:
`console.log("Checking Library:", window.JSAmbisonics);`
* **Fix:** If the library object is undefined, the audio graph construction crashes silently. Ensure the script is actually loaded (try adding it via a `<script>` tag in `index.html` as a fallback if the React import is failing).

**4. The "Resume" Hard-Lock**

* **Requirement:** In your `handlePlay()` function, you must force the context to wake up:
```javascript
if (audioContext.state === 'suspended') {
    console.log("Context suspended. Attempting resume...");
    await audioContext.resume();
    console.log("Context state is now:", audioContext.state);
}

```



**Action:** Implement these 4 diagnostic steps immediately.

1. Add the Test Tone button.
2. Add the Debug Log.
3. Log the Library status.
4. Force Resume on Play.

**Report back:** Does the Test Tone beep? Does the Debug Log show the file loaded?

