# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
This execution phase addresses a visual and functional regression in the `AmbiRotate` module. While the custom `<Knob />` components were successfully restored in the previous phase, they are currently all rendering as radial dials. Based on the original design spec, these controls must visually represent their spatial axis: Yaw must be a horizontal slider, Pitch must be a vertical slider, and Roll must be a radial dial. 

## 2. Technical Decisions & Dependencies
* **Prop-Driven Rendering:** The `src/tools/AmbiRotate/components/Knob.tsx` file was originally engineered to handle multiple display types (horizontal, vertical, radial) via a specific TypeScript prop (likely named `variant`, `type`, `layout`, or `orientation`).
* **Strict Audit Requirement:** The agent must *not* guess the prop name. It must strictly read the `Knob.tsx` TypeScript interface to identify the correct enum/string literal union that controls the shape, and then apply those exact strings to the parent instances.

## 3. Implementation Phases

### Phase 1: Component Interface Audit & Prop Correction — ✅ Completed
* **Objective:** Identify the layout-controlling prop within the `Knob` component and inject the correct values into the `AmbiRotate` parent view.
* **Instructions:**
  1. **READ FIRST:** Open `src/tools/AmbiRotate/components/Knob.tsx` and read the TypeScript interface (e.g., `interface KnobProps`).
  2. Identify the prop responsible for the visual shape. It will likely have a type signature similar to `'horizontal' | 'vertical' | 'radial'`.
  3. Open `src/tools/AmbiRotate/index.tsx`.
  4. Locate the three `<Knob />` instances. 
  5. **Update the Yaw Knob:** Inject the prop to render it as a **horizontal** slider. Ensure the color prop remains mapped to Blue.
  6. **Update the Pitch Knob:** Inject the prop to render it as a **vertical** slider. Ensure the color prop remains mapped to Green.
  7. **Update the Roll Knob:** Inject the prop to render it as a **radial/circular** dial. Ensure the color prop remains mapped to Red.
* **TDD Checkpoint:**
  * **Test Case 1:** [Given the `AmbiRotate` index component, When parsed, Then assert the Yaw `<Knob>` instance receives the horizontal variant prop].
  * **Test Case 2:** [Given the `AmbiRotate` index component, When parsed, Then assert the Pitch `<Knob>` instance receives the vertical variant prop].
  * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Audit the interface and correct the JSX props. Once verified, set status to ✅ Completed.
  * **HUMAN VALIDATION REQUIRED:** STOP. Ask the user to visually confirm that the AmbiRotate controls now display one horizontal slider (Yaw), one vertical slider (Pitch), and one radial dial (Roll).

## 4. Final Review & Validation
* **Technical Audit:** Verify that `src/tools/AmbiRotate/index.tsx` is passing three distinct layout variants to its children, and that no other structural changes were made to the file.
* **Status Reconciliation:** Verify Phase 1 is marked ✅.
* **Final Human Sign-off:** Present the finalized build to the user and request visual confirmation of the slider orientations.