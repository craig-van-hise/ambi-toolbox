
### PRP 43: AmbiRotate Standardization & Cleanup

**Objective:** Restore the missing Drop Zone/Queue to AmbiRotate, enforce the standard "Vertical Stack" layout, and clean up the visual clutter in the player controls.

> **Instruction for Agent:**
> **AmbiRotate** has deviated from the standard UI pattern. The Drop Zone is missing, and the controls are cluttered. You must refactor `src/tools/AmbiRotate/index.tsx` to match the exact structure of the other tools.
> **1. Enforce the Standard Layout Structure:**
> Replace the main content area with the **Standard Vertical Stack** (defined in PRP 6):
> ```tsx
> {/* Main Container: Flex Column */}
> <div className="flex-1 min-h-0 flex flex-col p-4 gap-4 overflow-hidden">
> 
> ```
> 
> 

> ```
> {/* 1. DROP ZONE (Top - Flexible) */}
> <div className="flex-1 min-h-0">
>     <SmartDropZone
>         className="h-full w-full"
>         onFilesLoaded={handleFiles}
>         {/* Ensure this supports the 'onDrop' legacy prop too */}
>     />
> </div>
> 
> ```
> 
> 

> ```
> {/* 2. QUEUE (Middle - Rigid) */}
> {files.length > 0 && (
>     <div className="flex-none max-h-48 overflow-y-auto bg-gray-800 rounded-lg border border-gray-700 p-4">
>         {/* Use the Standard QueueList component */}
>         <QueueList
>             files={files}
>             onRemove={removeFile}
>             activeFileIndex={currentFileIndex} // Highlight the playing file if supported
>         />
>     </div>
> )}
> 
> ```
> 
> 

> ```
> {/* 3. ROTATOR CONTROLS (Bottom - Tool Specific) */}
> {/* Only show this if a file is loaded/active */}
> {activeFile && (
>     <div className="flex-none bg-gray-900 rounded-xl border border-gray-700 p-6">
>         {/* YOUR ROTATOR COMPONENT GOES HERE */}
>         <RotatorInterface ... />
>     </div>
> )}
> 
> ```
> 
> 

> **2. Clean Up the Rotator Interface (Visual Fixes):**
> Inside the `RotatorInterface` (or whatever component holds the sliders/buttons):
> * **Delete the Title:** Remove the `<h2>AmbiRotate PRO</h2>` element and the refresh/cycle icon next to it. The tool already has a global title.
> * **Fix the File Info:** The text `TRACK 1/2: [Filename]` is currently jammed to the right.
> * Move this text to a distinct line *above* the controls.
> * Style it as: `text-sm text-gray-400 mb-4 truncate`.
> 
> 
> * **Update Label:** Locate the text `"STEREO MONITOR"` and change it to **"BINAURAL MONITOR"**.
> * **Alignment:** Ensure the "Upside Down", "Rotate 180", "Lay Flat" buttons are aligned to the left, and "Reset" is aligned to the right.
> 
> 
> **3. Outcome:**
> AmbiRotate must look exactly like Ambix2IAMF/Ambix2Opus (Drop Zone on top, Queue below it) but with the Rotator controls appearing at the bottom when a file is selected.