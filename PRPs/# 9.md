
**Role:** Senior Full-Stack Architect (Electron/React/TypeScript)

**Status:**
**CRITICAL FIX REQUIRED.** The previous implementation of `AmbiRotate` failed on three specific points. You must correct them immediately.

**1. Fix the Transport (UI Restore)**

* **Defect:** You removed the scrubber and loop controls.
* **Requirement:** Restore the **Full Transport** functionality in `src/tools/AmbiRotate/index.tsx`.
* **Scrubber:** Add a range slider (progress bar) bound to the audio's current time. It must be draggable to seek.
* **Loop:** Add a **Loop Toggle** button. When active, the playback must cycle the selected region (or whole file) to allow the user to fine-tune the center.
* **Controls:** Play, Pause, Stop, Loop (Toggle), Scrubber.



**2. Fix the Presets Logic (Mutually Exclusive  Additive Toggles)**

* **Defect:** The buttons currently overwrite each other (Radio logic).
* **Requirement:** Change "Upside Down", "Rotate 180", and "Lay Flat" to **Independent Toggle Buttons** (On/Off).
* **Logic:** These are *modifiers* to the base rotation, not replacements.
* *Equation:* `Final Roll = Slider_Roll + (isUpsideDown ? 180 : 0)`
* *Equation:* `Final Yaw = Slider_Yaw + (isRotated180 ? 180 : 0)`
* *Equation:* `Final Pitch = Slider_Pitch + (isLayingFlat ? 90 : 0)`


* **UX:** The visualizer (Compass/Head) must reflect the **Sum** of the Slider + Toggles.

**3. Fix the Backend Data Type (File Size Inflation)**

* **Defect:** The output file is ~25-33% larger than the input.
* *Cause:* You are likely loading the audio as Int24 (or similar) but saving the NumPy array as **Float32** (default for `soundfile`/`scipy`).


* **Requirement:** Update `resources/scripts/rotate_ambisonics.py`.
1. **Detect Input Subtype:** Read the subtype of the source file (e.g., `'PCM_24'`, `'PCM_16'`, `'FLOAT'`).
2. **Match Output:** Force the writer to use the **exact same subtype**.
3. *Constraint:* If the input is 24-bit, the output **must** be 24-bit. Do not promote to Float32 unless the input was Float.



**Action:** Apply these 3 fixes now. Do not alter other parts of the codebase.