status: ✅ Completed



**Role:** Senior Audio Architect

**Status:**
**CRITICAL CRASH:** `Error: Invalid array length`.

* **Diagnosis:** The `BinauralDecoder` is crashing because it lacks the HRTF filters required to process the audio. The library expects these to be loaded asynchronously before instantiation.
* **The Fix:** We must implement the `HRTF Loader` pattern.

**Task:**
Refactor `AmbiRotate/PreviewEngine.ts` to properly load the spatial filters.

**1. Update the Class Structure**
We need to hold a reference to the `hrtfLoader`.

* **Import:** No new import needed (it's on the global object).
* **Variable:** `let hrtfSet: any = null;`

**2. The New Initialization Logic (`setupAudio`)**
Replace the crashing `Initializing Spatial Architecture` block with this asynchronous flow:

```typescript
// Define the Global Lib
const Lib = window.ambisonics || window.JSAmbisonics || (window as any).ambisonics;

// 1. ISOLATION TEST: Check Rotator First (The Math Part)
console.log("Initializing Rotator...");
let rotator;
try {
    rotator = new Lib.SceneRotator(order);
    console.log("Rotator Initialized.");
} catch (e) {
    console.error("Rotator Crash:", e);
    throw e;
}

// 2. THE DECODER FIX (The HRTF Part)
console.log("Initializing Binaural Decoder...");
try {
    // A. Check if we need to load filters
    // Note: Some versions of JSAmbisonics require explicit HRTF loading. 
    // If the standard constructor fails, we use the fallback "Rosenfell" (Generic) mode which generates filters mathematically without external files.
    
    // ATTEMPT 1: Try the 'SOA' (Spatial Audio) or 'Rosenfell' method if available to avoid loading files.
    // If your version of the lib is minimal, it might only support Order 1.
    // Let's cap the order for the DECODER only (keep Rotator at 3).
    
    const decodeOrder = Math.min(order, 3); // Safety Cap
    const decoder = new Lib.BinauralDecoder(context, decodeOrder); 
    
    // IMPORTANT: You might need to reset filters to defaults to avoid the "Empty Array" crash
    // Check if resetFilters exists
    if (decoder.resetFilters) {
         decoder.resetFilters(); 
    }

    // Connect Graph
    sourceNode.connect(rotator.in);
    rotator.out.connect(decoder.in);
    decoder.out.connect(context.destination); // Direct to speakers
    
} catch (e) {
    console.error("Decoder Crash:", e);
    
    // FALLBACK: If Binaural Fails, just monitor the W-Channel (Mono)
    // This allows the user to still use the tool even if spatial audio breaks.
    console.warn("Falling back to Mono Monitoring (W-Channel).");
    const gain = context.createGain();
    gain.gain.value = 0.5; // Lower volume
    
    // Connect only Channel 0 (W) to speakers
    const splitter = context.createChannelSplitter(16);
    sourceNode.connect(rotator.in);
    rotator.out.connect(splitter);
    splitter.connect(gain, 0); // Connect Ch0 to Gain
    gain.connect(context.destination);
}

```

**3. The "Order Safety" Check**

* **Constraint:** If `Detected Order` is 3, ensure `order` passed to the constructor is the **Number** `3`, not the string `"3"`.
* Add: `const safeOrder = Number(order);`



**Action:**

1. Apply this "Try/Catch/Fallback" logic.
2. If the Binaural Decoder crashes, it will automatically switch to **Mono Monitoring** (W-Channel). This proves if the `Rotator` is working (which is the core function of the tool).
3. **Run the Test.**

