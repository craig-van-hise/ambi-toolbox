status: ✅ Completed


### Product Requirements Prompt (PRP): URL Parameter & Intent Fix


> **CRITICAL BUG FIX: MISSING URL PARAMETERS & INTENT STATE**
> Your previous implementation successfully ordered the steps, but you introduced a fatal syntax error. You are omitting the `channels` query parameter from the final stream URL, causing a `400 Bad Request` from the backend. Additionally, double-clicks are registering as `shouldPlay: false`.
> **EXECUTION RULES:**
> Do not refactor the architecture. Fix the exact string construction and state booleans. Use strict TDD.
> ---
> 
> 
> ### Phase 1: Fix the URL Template Literal (Frontend)
> 
> 
> **CONTEXT:** Step 3's log shows `Committing Stream: ...?file=...&start=0`. It is completely missing `&channels=...`.
> **TASK:** In `src/contexts/PlaybackContext.tsx`, locate the Step 3 `useEffect` that constructs the `/obr-stream` URL.
> 1. Inject the parsed channel count into the URL string: `&channels=${probeData.channels}`.
> 2. Ensure `profile=ambient` (or the correct profile state) is also appended if required by the backend.
> 
> 
> **TEST:** Write a simple pure-function unit test (`scripts/test-url-string.ts`) that takes a mock `probeData = { channels: 16 }` and asserts that the resulting URL string explicitly `.includes('&channels=16')`.
> **CHECKPOINT:** Do not proceed to Phase 2 until the URL string test is green.
> ---
> 
> 
> ### Phase 2: Fix the Double-Click Intent (Frontend)
> 
> 
> **CONTEXT:** When double-clicking a file in the queue, Step 1 logs `shouldPlay: false`. It must be `true` to trigger auto-play.
> **TASK:** > 1. Locate the `onDoubleClick` handler in your Queue/FileList component (or the function it calls in `PlaybackContext`).
> 2. Explicitly set the intent to `true` (e.g., `setPlaybackIntent(true)` or `shouldAutoPlay.current = true`) the moment the double-click registers.
> **REPORTING:** Do not report back until the URL string is fixed. Output the console log of a successful Step 3 commit containing the `channels` parameter.

