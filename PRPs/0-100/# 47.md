status: ✅ Completed


# 🚨 CRITICAL CORRECTION: Bitrate Logic is Wrong

**STOP.** The current implementation uses **Fixed Total Bitrates** (256/512/1024 kbps). This is **incorrect** and breaks the tool for higher-order Ambisonics (e.g., 36 channels at 1024kbps is unlistenable garbage quality).

**Requirement:**
You must implement **Per-Channel Bitrate** logic. The user selects quality *per channel*, and the system calculates the total.

### 1. Fix Frontend (`ToolViews.tsx`)
status: ✅ Completed

Change the dropdown options to these exact values:

* **Low** (64 kbps per channel)
* **Medium** (96 kbps per channel)
* **High** (128 kbps per channel)
* **Pro** (192 kbps per channel)

*Do not display total kbps in the UI. Display the per-channel value.*

### 2. Fix Backend (`main.swift` & `Ambix2APAC.ts`)
status: ✅ Completed

The Swift binary must accept the **per-channel** integer (e.g., `96000`) and perform the math internally before configuring `AVAssetWriter`:

```swift
// Pseudo-code requirement for main.swift
let channelCount = inputFormat.channelCount
let perChannelBitrate = args.perChannelBitrate // e.g. 96000
let totalBitrate = perChannelBitrate * channelCount 

// APPLY THE TOTAL
outputSettings[AVEncoderBitRateKey] = totalBitrate

```

**Execute this fix immediately. Do not use fixed totals.**