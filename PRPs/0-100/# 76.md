status: ✅ Completed



# Product Requirements Prompt (PRP): DSP Math Correction & Input Override

## 1. The Critical DSP Bug (The Radian Error)
status: ✅ Completed

Your previous fix successfully stabilized the `pan` filter parser (the `0*c0` trick was correct). However, the spatial audio is completely scrambled because you fed **degrees** directly into JavaScript's `Math.sin()` and `Math.cos()` functions.

You must explicitly convert the grid's Azimuth and Elevation from degrees to radians before calculating the Spherical Harmonics (SH).

## 2. Action 1: Fix the Matrix Math (ACN / SN3D)
status: ✅ Completed

Update your `matrix_utils.ts` to enforce strict Radian conversion and AmbiX ACN ordering. Use this exact logic for the first 4 channels (Order 1) to ensure your axes are correct, and extrapolate Orders 2 and 3 using the same radian variables:

```javascript
// MUST convert degrees to radians
const azRad = node.az * (Math.PI / 180);
const elRad = node.el * (Math.PI / 180);

// AmbiX ACN Ordering & SN3D
const c0 = 1.0;                                     // W (Omni)
const c1 = Math.sin(azRad) * Math.cos(elRad);       // Y (Left/Right)
const c2 = Math.sin(elRad);                         // Z (Up/Down)
const c3 = Math.cos(azRad) * Math.cos(elRad);       // X (Front/Back)

// Apply node weight
const weight_c0 = c0 * node.w;
const weight_c1 = c1 * node.w;
// ... etc

```

## 3. Action 2: Override FFmpeg's Input Layout Guess
status: ✅ Completed

FFmpeg is incorrectly guessing that our 16-channel Ambisonic input is a discrete `hexadecagonal` surround file, which alters the channel mapping before the filtergraph.

* **The Fix:** In your `child_process.spawn` arguments, you must add `-ac 16` **BEFORE** the `-i` flag. This explicitly tells the demuxer to ingest the file as 16 raw channels, bypassing the layout guesser.

## 4. TDD Verification
status: ✅ Completed

1. Apply the Radian conversion to all 16 channels in your matrix utility.
2. Add the `-ac 16` input flag.
3. Run your `scripts/test-binaural.js` script to ensure the pipeline still compiles.
4. Do not report back until the test passes.

