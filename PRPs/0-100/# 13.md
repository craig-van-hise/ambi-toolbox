status: ✅ Completed



**Role:** Senior Frontend Engineer (React/Electron)

**Status:**
**ROOT CAUSE IDENTIFIED: Missing Dependency.**
The diagnostic log shows `JSAmbisonics: MISSING`. The `import` statement is failing silently, likely due to bundler (Vite/Webpack) incompatibility with this legacy library.

**Task:**
Switch to the **"Static Vendor Strategy"** to ensure `JSAmbisonics` loads globally.

**1. Vendor the Library (Manual Integration)**

* **Step A:** Locate the `jsambisonics.js` (or `.min.js`) source code. (If you cannot find it in node_modules, you must create it in `public/lib/jsambisonics.js` using the source from the official GitHub repository: `polarch/JSAmbisonics`).
* **Step B:** Place this file strictly in the **Public Static Assets folder** (e.g., `public/` or `static/`, depending on your Vite/Electron setup).
* *Target Path:* `public/js/JSAmbisonics.min.js`



**2. Update `index.html` (The Global Injection)**

* Open your `index.html` (the entry point for React).
* Add a script tag to the `<head>` or body:
```html
<script src="/js/JSAmbisonics.min.js"></script>

```


* *Verification:* This forces the browser to load the library before React starts, attaching it to `window.JSAmbisonics`.

**3. Update `PreviewEngine.ts` / `index.tsx**`

* **Remove** the failed top-level import: `import * as JSAmbisonics from ...` (Delete this).
* **Use the Window Object:**
* In your TypeScript file, define the type on the window to stop linter errors:
```typescript
declare global {
    interface Window {
        JSAmbisonics: any;
    }
}

```


* Initialize the rotator using the global:
```typescript
// OLD: new JSAmbisonics.SceneRotator(...)
// NEW: new window.JSAmbisonics.SceneRotator(...)

```





**4. Check the "Proxy" Logic**

* Ensure that when you initialize the `SceneRotator`, you are passing the **Order** correctly.
* *Recall:* If the input file is >3rd Order, you must initialize the Rotator as `3rd Order` and only feed it the first 16 channels of the buffer.

**Action:** Execute this "Static Script" refactor immediately. This bypasses the build system and guarantees the library exists.

