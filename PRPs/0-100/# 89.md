status: ✅ Completed


### Product Requirements Prompt (PRP): Integration Bug Fixes (Phased TDD)


> **OBJECTIVE:** Fix the fatal path resolution error for the `obr_stream` binary and squash the `channels=4` regression in the React frontend.
> **EXECUTION RULES:**
> You must execute this task using strict Test-Driven Development (TDD) across two distinct phases. You MUST write and execute the specified test at the end of each phase. You are strictly forbidden from proceeding to the next phase until the current phase's test yields a passing result (green light).
> ---
> 
> 
> ### Phase 1: Fix Binary Path Resolution (Backend)
> 
> 
> **CONTEXT:** The server is crashing because `getObrStreamPath` is returning an incorrect path outside the project root (`.../src/cpp/build/obr_stream`). It must resolve to the same local `assets/bin/` directory as FFmpeg.
> **TASK:** Modify the path resolution logic in `ObrHandler.ts` (or wherever `getObrStreamPath` lives).
> 1. Use `path.join` to resolve the `obr_stream` binary to the exact same `assets/bin/` directory where the `ffmpeg` binary is successfully located.
> 2. Ensure it accounts for the Electron `app.getAppPath()` or `process.resourcesPath` correctly for both dev and production environments.
> **TEST:** Write and run `scripts/test-binary-paths.ts`. This script must import your path resolution functions and use `fs.existsSync()` to assert that BOTH the `ffmpeg` and `obr_stream` binaries exist at the resolved paths.
> **CHECKPOINT:** Stop and evaluate. Do not proceed to Phase 2 until this test passes.
> 
> 
> ---
> 
> 
> ### Phase 2: Fix Dynamic Channels Regression (Frontend)
> 
> 
> **CONTEXT:** The `PlaybackContext.tsx` is still requesting `/obr-stream?...&channels=4` even when a 16-channel file is loaded.
> **TASK:** Audit `src/contexts/PlaybackContext.tsx`.
> 1. Locate the exact function firing the stream URL when the user presses play or loads a file.
> 2. Ensure the `channels` query parameter is dynamically pulled from the `WavDecoder`'s metadata state, NOT hardcoded to `4` or defaulted incorrectly.
> **TEST:** Write `scripts/test-frontend-url.ts`. Mock the state of a loaded 16-channel file, pass it through the URL generation logic used in `PlaybackContext.tsx`, and assert that the resulting string contains `channels=16`.
> **CHECKPOINT:** Report back ONLY when Phase 2 is complete and all tests are green.
> 
> 

---
