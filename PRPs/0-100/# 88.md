
### Product Requirements Prompt (PRP): Un-Mocking the Pipeline (PRP #88)

> **OBJECTIVE:** Resolve the hanging 3-stage pipeline and replace the mocked functional test with a passing, end-to-end data test. Static analysis is strictly forbidden for verifying this pipeline.
> **CONTEXT:** > In the previous task, you mocked the functional test for the 3-stage pipeline because the environment was hanging/crashing. This violates the strict TDD mandate. The pipeline is likely deadlocking due to unconsumed `stderr` buffers from FFmpeg, or incorrect `stdio` configurations in the `spawn` commands.
> **EXECUTION RULES:**
> Execute this using strict TDD across two phases. You must not proceed to Phase 2 until Phase 1's functional test yields a valid, playable WebM file > 0KB.
> ---
> 
> 
> ### Phase 1: Fix Process Deadlocks & Stream Handlers
> 
> 
> **TASK:** Modify the 3-stage pipeline in `ObrHandler.ts` to guarantee it does not deadlock.
> 1. **Stdio Config:** Ensure all three processes (`decoder`, `obr`, `encoder`) are spawned with `stdio: ['pipe', 'pipe', 'pipe']`.
> 2. **Drain Stderr:** You MUST attach `.on('data')` listeners to the `stderr` of all three processes. Log the output to the console for debugging, which will also drain the OS buffers and prevent freezing.
> 3. **Error Handling:** Attach `.on('error')` and `.on('close')` listeners to all processes. If any process exits with a non-zero code, cleanly destroy all streams and call `res.end()`.
> 
> 
> **TEST:** Write and run `scripts/test-live-pipeline.ts`.
> * Spin up the test server.
> * Send a GET request to `/obr-stream` with a valid test WAV file.
> * Pipe the HTTP response stream to a local file (`test_output.webm`).
> * Let it run for 3 seconds, then abort the request.
> * Assert that `test_output.webm` exists on disk and its file size is strictly greater than 10KB.
> **CHECKPOINT:** Stop and evaluate. If the test hangs, read the `stderr` logs you attached to fix the crash. Do not proceed until `test_output.webm` is successfully written.
> 
> 
> ---
> 
> 
> ### Phase 2: Teardown & UI Verification Prep
> 
> 
> **TASK:** Now that the pipeline functionally works without hanging, ensure the backend cleans up perfectly.
> 1. In `ObrHandler.ts`, verify that when the `req.on('close')` or `req.on('aborted')` event fires (simulating the UI changing a file or pausing), all three child processes are aggressively killed (e.g., using `kill('SIGKILL')`).
> 2. Ensure no zombie `ffmpeg` or `obr_stream` processes remain in the system after the test script completes.
> 
> 
> **REPORTING:** Report back ONLY when Phase 1 yields a valid WebM file > 10KB and Phase 2 teardown is implemented. Include the file size of `test_output.webm` in your final response.
