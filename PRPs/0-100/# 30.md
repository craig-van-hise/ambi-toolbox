status: ✅ Completed



**Role:** Senior Frontend Engineer (UX/React)

**Status:**
**UX REPAIR REQUIRED.**
**Regressions:**

1. **Quick Buttons Gone:** The "Upside Down", "Flip", etc. buttons were erased.
2. **Auto-Play:** The engine auto-starts on load (User wants: Load  Ready  Manual Play).
3. **Transport/Looping:**
* Looping needs visual **"Flags"** (Left/Right locators) above the timeline, not just buttons.
* Scrubber knob desyncs during looping.
* Play button state is confusing.



**Task:**
Overhaul the `AmbiRotate` UI to restore features and implement a professional Transport.

**1. Restore Quick Action Buttons**

* **Location:** Add a row *above* or *below* the sliders (wherever they fit best in the original design).
* **Components:**
* `[Upside Down]`: Toggles Roll 180.
* `[Rotate 180]`: Toggles Yaw 180.
* `[Lay Flat]`: Toggles Pitch 90.
* `[Reset]`: Resets all to 0.


* **Logic:** These must update the Sliders and the Engine immediately.

**2. Fix Transport Logic (No Auto-Play)**

* **PreviewEngine:** Remove `source.start()` from the loading sequence.
* Instead, set a flag `isReady = true`.
* Enable the Play button in the UI.


* **Play Button:** Only active when `isReady` is true.
* **Stop Button:**
* **Behavior:** Stops audio AND resets the scrubber to `Loop Start` (or 0).
* **Visual:** Distinct from Pause.



**3. Implement "Pro" Loop Timeline**

* **Structure:** Create a generic `Timeline` component.
* **Container:** `relative` div.
* **Loop Flags:** Two absolute-positioned elements (triangles/arrows) above the track.
* **Left Flag (`loopIn`):** `left: ${loopInPercent}%`. Draggable.
* **Right Flag (`loopOut`):** `left: ${loopOutPercent}%`. Draggable.


* **Scrubber:** The standard range input below the flags.
* **Loop Region:** A semi-transparent colored `div` behind the scrubber between In/Out points.


* **Sync Logic:**
* In the `requestAnimationFrame` loop:
* If `currentTime >= loopOut`, visually snap slider back to `loopIn`.
* (The Audio Engine handles the actual audio looping, the UI just needs to match).





**4. "Instant" Playability (Partial Loading)**

* *Note:* Truly instant playback for 500MB WAVs requires a streaming decoder (complex).
* **Optimization:** Ensure the **Progress Bar** is prominent so the user knows exactly why they are waiting.
* **Interaction:** The Play button should be `disabled` (greyed out) with a "Loading..." text until the buffer is ready, preventing "impatience clicks".

**Implementation Details (`index.tsx`)**

* **State:**
```typescript
const [loopIn, setLoopIn] = useState(0); // Time in seconds
const [loopOut, setLoopOut] = useState(duration); 
const [isLooping, setIsLooping] = useState(false);

```


* **Visuals:**
* Use SVG icons for the Loop Flags (▼ for In, ▼ for Out).
* Ensure the flags don't overlap or cross each other.



**Action:**

1. Disable Auto-Play in `PreviewEngine`.
2. Implement the Quick Buttons row.
3. Build the **Timeline Component** with Loop Flags.
4. Fix the Scrubber Sync loop.

