

**Role:** Senior Frontend Designer

**Status:**
**UI POLISH & RESTORATION.**
**Tasks:**

1. **Timeline:** Change Right Loop Locator color to **Green** (fixing the "Red" issue).
2. **Action Area:** Restore the missing **Render Button** with the correct Red styling to match the tool's theme.

**Step 1: Update Loop Colors (`src/tools/AmbiRotate/components/Timeline.tsx`)**
We will change the Right Flag's border color to green.

* **Edit the JSX for the Loop Flags:**

```tsx
            {/* Left Flag (In) - Keep current accent (or make Green if you want matched pair?) */}
            {/* User specifically asked for RIGHT to be Green. We will make Right Green. */}
            
            <div 
                className="absolute top-0 w-0 h-0 border-t-[8px] border-t-transparent border-b-[8px] border-b-transparent border-l-[12px] border-l-studio-accent cursor-ew-resize hover:scale-110 transition-transform z-20"
                style={{ left: `calc(${pct(loopIn)}% - 0px)` }} 
                onMouseDown={(e) => { e.stopPropagation(); setDragging('in'); }}
                title="Loop Start"
            />

            {/* Right Flag (Out) - GREEN */}
            <div 
                className="absolute top-0 w-0 h-0 border-t-[8px] border-t-transparent border-b-[8px] border-b-transparent border-r-[12px] border-r-green-500 cursor-ew-resize hover:scale-110 transition-transform z-20"
                style={{ left: `calc(${pct(loopOut)}% - 12px)` }} 
                onMouseDown={(e) => { e.stopPropagation(); setDragging('out'); }}
                title="Loop End"
            />

```

*(Note: If `border-r-green-500` doesn't show, ensure your Tailwind config includes green. It is standard, so it should work).*

**Step 2: Restore Render Button (`src/tools/AmbiRotate/index.tsx`)**
We need to add the "Action Bar" at the bottom of the component, just like the other tools.

* **Locate the bottom of your JSX return (after the Timeline/Transport).**
* **Insert this Block:**

```tsx
            {/* --- ACTION AREA --- */}
            <div className="mt-8 pt-6 border-t border-studio-panel-light flex items-center justify-between">
                
                {/* Status Text (Optional) */}
                <div className="text-sm text-gray-400">
                    {/* Placeholder for status or file info */}
                    <span>{files.length > 0 ? `${files.length} file(s) ready` : 'No files loaded'}</span>
                </div>

                {/* RENDER BUTTON (Red) */}
                <button
                    onClick={handleRender} 
                    disabled={files.length === 0 || isProcessing}
                    className={`
                        px-8 py-3 rounded-lg font-bold shadow-lg flex items-center gap-2 transition-all
                        ${files.length === 0 || isProcessing
                            ? 'bg-gray-700 text-gray-500 cursor-not-allowed'
                            : 'bg-red-600 hover:bg-red-500 text-white hover:scale-105 active:scale-95 shadow-red-900/50'
                        }
                    `}
                >
                    {isProcessing ? (
                        <>
                            {/* Spinner SVG */}
                            <svg className="animate-spin h-5 w-5 mr-2 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                                <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4"></circle>
                                <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                            </svg>
                            Rendering...
                        </>
                    ) : (
                        <>
                            {/* Download/Export Icon */}
                            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
                            </svg>
                            Render Rotated File(s)
                        </>
                    )}
                </button>
            </div>

```

**Step 3: Define `handleRender` (If missing)**
Ensure you have the handler function defined in `index.tsx`:

```typescript
    const [isProcessing, setIsProcessing] = useState(false);

    const handleRender = async () => {
        if (files.length === 0) return;
        setIsProcessing(true);
        try {
            // Call the Backend to process the file with current Yaw/Pitch/Roll
            // We pass the rotation values to the Python backend
            const result = await window.electronAPI.processAmbiRotate(
                files.map(f => f.path), 
                { yaw, pitch, roll }
            );
            console.log("Render Complete:", result);
            // Show Success Notification?
        } catch (error) {
            console.error("Render Failed:", error);
        } finally {
            setIsProcessing(false);
        }
    };

```

**Action:**

1. Update `Timeline.tsx` to set the right flag color.
2. Add the Render Button block to `index.tsx`.
3. Ensure `handleRender` is wired up.