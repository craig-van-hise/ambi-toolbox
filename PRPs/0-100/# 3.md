status: ✅ Completed



**Role:** Senior Full-Stack Architect (Electron/React/TypeScript)

**Current Status:**

* `Ambix2Opus`, `Ambix2IAMF`, `Ambix2Bin`, and `Ambix2CAF` are **COMPLETE**.
* We are now activating **AmbiOrder** (Blue).

**Task:**
**Wire up** the existing Frontend UI for `AmbiOrder` to the new Backend Logic.

**1. Frontend Strategy (`src/tools/AmbiOrder/index.tsx`)**

* **DO NOT** redesign the UI layout. Use the existing JSX structure from the "Front End Design".
* **DO** replace the hardcoded "Detected Input: 3rd Order" text with a **React State variable**.
* **Logic:**
* On Drop  Call `inspectFile(path)` (New Backend Function).
* On Result  Update State `detectedOrder`.
* **Dropdown Logic:** Dynamically filter the "Target Output" options. If input is 2nd Order, remove "3rd Order" from the list.



**2. Backend Strategy (`electron/handlers/ambiorder.ts`)**

* **Function A: `inspectFile**`
* Use `ffprobe` to get the channel count.
* Return the Order ().


* **Function B: `convertFile` (The Truncation)**
* **CRITICAL CONSTRAINT:** Do **NOT** use standard downmixing (`-ac 4`).
* **Use the `pan` filter:** You must explicitly keep the first  channels and discard the rest to preserve spatial accuracy.
* *Command:* `ffmpeg -i input.wav -filter_complex "pan=TargetChannels|c0=c0|...|cN=cN" output.wav`



**3. Action Plan**

1. Write `electron/handlers/ambiorder.ts`.
2. Update `preload.ts` to expose the file inspection capability.
3. Modify `src/tools/AmbiOrder/index.tsx` to use `useState` instead of static text.

**Execute this integration now.**