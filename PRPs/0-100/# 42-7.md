status: ✅ Completed


### PRP 42-7: Fix Queue Padding & Spacing

**Objective:** Add internal "breathing room" to the Queue box so text does not touch the borders.

> **Instruction for Agent:**
> The `QueueList` component currently lacks internal padding, causing the text ("QUEUE (2)", file names, etc.) to jam against the white border.
> **1. Update `src/components/QueueList.tsx`:**
> * Locate the **outermost wrapper `div**` (the one with the border and rounded corners).
> * **Add the class `p-4**` (padding: 1rem).
> * *Current behavior:* `border border-gray-700 rounded-lg`
> * *New behavior:* `border border-gray-700 rounded-lg p-4`
> 
> 
> 
> 
> **2. Check Internal Alignment:**
> * Ensure the "Header" row (containing "QUEUE (N)" and "Clear All") and the "List Items" below it align perfectly on the left edge.
> * If the list items have their own padding, adjust it so they don't look indented double relative to the header. Ideally, remove extra margin on the items and let the container's `p-4` handle the spacing.
> 
> 
> **3. Visual Goal:**
> * There should be a visible gap (approx 16px) between the white border and any text inside the box.
> 
>