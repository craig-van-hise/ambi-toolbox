status: ✅ Completed


**Role:** Senior Frontend Engineer

**Status:**
**FATAL ERROR IDENTIFIED:** `JSAmbisonics.SceneRotator is not a constructor`.
The library is loaded, but we are calling the wrong Class Name, so the audio graph crashes immediately.

**Task:**
Fix the Instantiation Logic and Verify the Binaural Chain.

**1. The "Probe" (Find the Real Name)**
We need to see exactly what the library exports.

* **Action:** In `src/tools/AmbiRotate/PreviewEngine.ts`, inside `setupAudio`, replace the initialization code with this **Debug Block**:
```typescript
// 1. inspect the object structure
const Lib = window.ambisonics || window.JSAmbisonics || (window as any).ambisonics;
console.log("DUMPING LIBRARY STRUCTURE:", Lib);

// 2. Safety Check
if (!Lib) throw new Error("Library object is null");

// 3. Find the Rotator (Try common variations)
// Some versions use lowercase 's', some use 'SceneRotator', some hide it in 'default'
const RotatorClass = Lib.SceneRotator || Lib.sceneRotator || Lib.default?.SceneRotator;
const DecoderClass = Lib.BinDecoder || Lib.binDecoder || Lib.BinauralDecoder || Lib.default?.BinauralDecoder;

if (!RotatorClass) throw new Error("Could not find Rotator class in library dump");
if (!DecoderClass) throw new Error("Could not find BinauralDecoder class in library dump");

// 4. Initialize
console.log("Initializing Spatial Chain...");
const rotator = new RotatorClass(order);
const decoder = new DecoderClass(order); // This is your Binaural Monitor

```



**2. The "Chain" (Verify Binaural Routing)**
You expressed concern that the agent "doesn't understand" we need binaural. Let's force it.

* **The Path Must Be:**
`Source (16ch)`  `Rotator (16ch)`  `Binaural Decoder (16ch -> 2ch)`  `Gain`  `Destination (Speakers)`.
* **Action:** Ensure the `connect` logic looks exactly like this:
```typescript
// Connect the Graph
sourceNode.connect(rotator.in);
rotator.out.connect(decoder.in);
decoder.out.connect(gainNode);
gainNode.connect(audioContext.destination);

// Reset HRTF (standard generic filters for monitoring)
decoder.resetFilters(); 

```



**Action:** Update `PreviewEngine.ts` with this robust "Probe & Connect" logic.

