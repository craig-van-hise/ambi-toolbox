status: ✅ Completed


**Role:** Senior Lead Architect

**Status:**
**EMERGENCY FIX: RESTORE LEGACY BRIDGE.**
**Root Cause:** The `preload.ts` file was updated to expose `electronAPI` for AmbiRotate, but in doing so, it removed `window.electron`, which the legacy tools (Epics, Opus, etc.) require to function.
**Goal:** Restore `window.electron.runTask` while keeping the new AmbiRotate functionality.

**Task:**
Replace `electron/preload.ts` entirely with this **Unified Bridge**.

**1. The Fix (`electron/preload.ts`)**
This file now exposes **TWO** separate APIs to the window: one for the old tools (`electron`) and one for the new tool (`electronAPI`). This ensures backward compatibility while supporting the new features.

```typescript
import { contextBridge, ipcRenderer } from 'electron';

// --- LEGACY BRIDGE (For EpicsToBin, MixToOpus, etc.) ---
contextBridge.exposeInMainWorld('electron', {
    // The critical function missing:
    runTask: (command: string, args: any[]) => ipcRenderer.invoke('run-task', command, args),
    
    // Other common legacy handlers (restoring just in case):
    selectFolder: () => ipcRenderer.invoke('dialog:openDirectory'),
    selectFile: () => ipcRenderer.invoke('dialog:openFile'),
    onLog: (callback: (event: any, msg: string) => void) => {
        const sub = (_: any, msg: string) => callback(_, msg);
        ipcRenderer.on('backend-log', sub);
        return () => ipcRenderer.removeListener('backend-log', sub);
    }
});

// --- NEW BRIDGE (For AmbiRotate) ---
contextBridge.exposeInMainWorld('electronAPI', {
    // Dialogs
    selectFiles: () => ipcRenderer.invoke('dialog:openFile'),
    expandPaths: (paths: string[]) => ipcRenderer.invoke('app:expandPaths', paths),
    
    // IPC for AmbiRotate
    processAmbiRotate: (filePaths: string[], rotation: any) => 
        ipcRenderer.invoke('process-ambi-rotate', filePaths, rotation),

    // Progress Listener
    on: (channel: string, callback: (data: any) => void) => {
        const subscription = (_: any, data: any) => callback(data);
        ipcRenderer.on(channel, subscription);
        return () => ipcRenderer.removeListener(channel, subscription);
    },
    
    // Audio Processing Listener (if used elsewhere)
    onProgress: (callback: (percent: number) => void) => {
        const sub = (_: any, p: number) => callback(p);
        ipcRenderer.on('file-read-progress', sub);
        return () => ipcRenderer.removeListener('file-read-progress', sub);
    }
});

```

**2. Verify `electron/main.ts**`
Ensure the `run-task` handler is actually present. It should be there (we didn't edit `main.ts` excessively), but if it was deleted, here is the restoration code for `main.ts`:

```typescript
// CHECK FOR THIS IN main.ts. IF MISSING, ADD IT:

ipcMain.handle('run-task', async (event, command, args) => {
    console.log(`[Main] Running task: ${command}`, args);
    // This connects to your central Python task runner (task_runner.py or similar)
    // We assume your legacy architecture uses a specific python script for this.
    // If you need me to restore the 'spawn' logic for this, let me know. 
    // Assuming the backend logic is:
    
    /* const script = path.join(__dirname, '../../py/task_runner.py'); 
    // ... spawn logic ...
    */
    
    // TEMPORARY CHECK: 
    // If you see "Running task" in your terminal, the connection is fixed.
});

```

*(Note: Usually `main.ts` retains its old code unless explicitly deleted. The error `window.electron.runTask is not a function` specifically points to `preload.ts` being the culprit).*

**Action:**

1. Overwrite `preload.ts` with the code above.
2. Restart the app (`npm run dev`)—**a full restart is required** for preload changes to take effect.
3. Test an old tool (e.g., Mix to Opus). It should work again.
4. Test AmbiRotate. It should *also* still work.