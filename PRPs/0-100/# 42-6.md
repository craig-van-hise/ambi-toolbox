status: ✅ Completed

### PRP 42-6: Enforce Vertical Stacking & Fix Overlap

**Objective:** Eliminate visual overlap by enforcing a rigid "Flex Column" layout. The Drop Zone, Queue, and Status Bar must be distinct siblings that share vertical space.

> **Instruction for Agent:**
> We have a critical UI bug where the **File Queue** and **Status Bar** are floating over the Drop Zone.
> You must refactor the "Middle Section" of **ALL TOOLS** to use a strict Vertical Flex Stack.
> **1. The Container Structure (Refactor Request)**
> Locate the main content area (between the Header and the Bottom Action Bar). Replace its structure with this exact hierarchy:
> ```tsx
> {/* MIDDLE SECTION WRAPPER: Handles the vertical distribution */}
> <div className="flex-1 min-h-0 flex flex-col p-4 gap-4 overflow-hidden">
> 
> ```
> 
> 

> ```
> {/* ITEM 1: The Drop Zone (Flexible Height) */}
> {/* logic: flex-1 allows it to fill all space, min-h-0 allows it to shrink */}
> <div className="flex-1 min-h-0">
>     <SmartDropZone
>         className="h-full w-full"
>         onFilesLoaded={handleFiles}
>         // ... other props
>     />
> </div>
> 
> ```
> 
> 

> ```
> {/* ITEM 2: The Queue (Constrained Height) */}
> {/* logic: flex-none prevents it from growing infinitely. max-h limits it so it doesn't crush the drop zone. */}
> {files.length > 0 && (
>     <div className="flex-none max-h-48 overflow-y-auto bg-gray-800 rounded-lg border border-gray-700">
>         <QueueList files={files} onRemove={removeFile} />
>     </div>
> )}
> 
> ```
> 
> 

> ```
> {/* ITEM 3: The Status Bar (Fixed Height) */}
> {/* logic: Standard block element at the bottom of the stack */}
> {statusMessage && (
>     <div className="flex-none p-3 bg-blue-900/30 border border-blue-500/30 text-blue-200 rounded text-sm">
>         {statusMessage}
>     </div>
> )}
> 
> ```
> 
> 

> **2. Clean Up Component CSS**
> * **Go to `QueueList.tsx`:** Remove **ANY** classes related to positioning: `absolute`, `fixed`, `z-index`, `bottom-0`, `left-0`. It must be a plain `<div>`.
> * **Go to `SmartDropZone.tsx`:** Ensure the container `div` has `h-full` so it fills the `flex-1` wrapper we created above.
> 
> 
> **3. Outcome Check:**
> * When the window shrinks, the **Drop Zone** must get shorter.
> * The **Queue** must always appear *below* the Drop Zone, never inside it or on top of it.
> * The **Status Bar** must appear *below* the Queue.
> 
>