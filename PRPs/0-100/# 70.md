status: ✅ Completed


# Product Requirements Prompt (PRP): Critical Workspace Recovery & Build Fix

## 1. Workspace State & Hallucination Correction
status: ✅ Completed

**CRITICAL CONTEXT:** Your internal workspace state is desynced from the actual file system.

* Do **NOT** look for `PRPs/# 90.md` or `PRPs/# 88.md`. They do not exist, and they are not open. Ignore them completely and drop them from your active memory.
* Do **NOT** run any Git commands.
* Do **NOT** run `npm install`.

Your sole objective right now is to fix two specific broken references so the Electron/React app can successfully compile and launch.

## 2. Action 1: Fix the Broken Import
status: ✅ Completed

There is a typo in the import paths causing a build failure.

* **File:** `src/components/Transport.tsx`
* **Task:** Locate the import for `formatTime`. It is currently pointing to `../utils/formatters`.
* **Fix:** Change it to point to the correct file on disk: `../utils/time-formatters`.

## 3. Action 2: Recreate the Missing Playback Context
status: ✅ Completed

The file `src/contexts/PlaybackContext.tsx` was deleted from the disk but is still referenced in `App.tsx` and `ToolViews.tsx`.

* **Task:** Create a new file at `src/contexts/PlaybackContext.tsx` and populate it with the following foundational code. Do not add any extra complex logic yet; just implement this exact stub to satisfy the compiler:

```tsx
import React, { createContext, useContext, useState, ReactNode } from 'react';

interface PlaybackContextType {
    isPlaying: boolean;
    togglePlayback: () => void;
    currentFile: string | null;
    setCurrentFile: (file: string | null) => void;
}

const PlaybackContext = createContext<PlaybackContextType | undefined>(undefined);

export const PlaybackProvider: React.FC<{ children: ReactNode }> = ({ children }) => {
    const [isPlaying, setIsPlaying] = useState(false);
    const [currentFile, setCurrentFile] = useState<string | null>(null);

    const togglePlayback = () => {
        setIsPlaying(!isPlaying);
    };

    return (
        <PlaybackContext.Provider value={{ isPlaying, togglePlayback, currentFile, setCurrentFile }}>
            {children}
        </PlaybackContext.Provider>
    );
};

export const usePlayback = () => {
    const context = useContext(PlaybackContext);
    if (context === undefined) {
        throw new Error('usePlayback must be used within a PlaybackProvider');
    }
    return context;
};

```

## 4. Acceptance Criteria
status: ✅ Completed

1. The import in `Transport.tsx` is corrected.
2. `PlaybackContext.tsx` is created and saved to disk.
3. You run the standard development command to launch the app.
4. The app compiles successfully without missing file errors.

Execute these steps immediately and confirm when the app is running.

