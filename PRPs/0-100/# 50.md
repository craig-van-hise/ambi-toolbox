status: ✅ Completed


> "The '9.1.6' alias suggests the metadata is being interpreted as channel-based rather than scene-based. Instead of OR-ing the tag with the channel count, please use the `AudioChannelLayout` struct to set the `mChannelLayoutTag` explicitly to `kAudioChannelLayoutTag_HOA_ACN_SN3D` (0x00BE0000) and ensure the `mNumberChannelDescriptions` is set to 0, as the tag itself implies the full HOA set. If `AVAssetWriter` still crashes, we need to provide an explicit `AudioChannelDescription` array mapping each channel to `kAudioChannelLabel_HOA_ACN_SN3D`."

### Verification Goal

When successful, `afinfo` should output:

* **Channel layout:** `Ambisonics` or `HOA_ACN_SN3D` (not "9.1.6").

Once that label appears, you have confirmed that the Vision Pro will treat the file as a true 3D sound field.

**Would you like me to provide the exact Swift code for the `AudioChannelDescription` mapping to ensure the agent doesn't get stuck in another crash loop?**