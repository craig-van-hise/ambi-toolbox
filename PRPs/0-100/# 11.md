status: ✅ Completed


**Role:** Senior Full-Stack Architect (Electron/React)

**Status:**
**CRITICAL FIX: Audio Preview is still silent.**
The previous method of loading audio (likely using `file://` URLs or `fetch`) is failing due to Electron/Chromium security policies (CORS/Local File Access).

**Task:**
Refactor the **File Loading Pipeline** for `AmbiRotate` to use the **IPC Bridge** instead of direct URL loading.

**1. The New Architecture (Read-Decode-Play)**
Do **NOT** use `new Audio(path)` or `fetch(path)`.
Implement this specific flow:

* **Step A: Backend (Main Process/Preload)**
* In `electron/handlers/fileHandlers.ts` (or similar), ensure you have a handler that uses Node's `fs` module to read the file.
* **Function:** `readFileAsBuffer(path: string): Promise<ArrayBuffer>`
* *Implementation:* `return fs.readFileSync(path).buffer;` (or async equivalent).
* **Bridge:** Expose this via `preload.ts` as `window.electronAPI.readFile(path)`.


* **Step B: Frontend (`src/tools/AmbiRotate/PreviewEngine.ts`)**
* **Load:** Call `const rawBuffer = await window.electronAPI.readFile(filePath);`
* **Decode:** Call `const audioBuffer = await audioContext.decodeAudioData(rawBuffer);`
* **Connect:** Pass this `audioBuffer` into your `JSAmbisonics` rotator node.



**2. Verify the Audio Graph (The Chain)**
Ensure the signal path is actually connected to the speakers:
`SourceNode (Buffer)`  `SceneRotator (JSAmbisonics)`  `BinauralConverter (JSAmbisonics)`  `GainNode (Volume)`  `AudioContext.destination`

**3. Debugging (Add Logs)**
Add these specific console logs in the loading function so we can diagnose where it stops:

* `console.log("Requesting file buffer from Main process...");`
* `console.log("Buffer received. Size:", rawBuffer.byteLength);`
* `console.log("Decoding audio data...");`
* `console.log("Decode success. Channels:", audioBuffer.numberOfChannels);`

**Action:** Refactor the file loading logic in `AmbiRotate` to use this `fs.readFile`  `IPC`  `decodeAudioData` pattern immediately.