### Product Requirements Prompt (PRP): The "Hard Swap" Track Transition



> **CRITICAL FAILURE CORRECTION:** > Your previous implementation (PRP #95) failed in the real-world UI. You stated "Manual Verification... works perfectly," but the player still gets permanently stuck at 0:00 when double-clicking a new track. The user has to manually press "Stop" and then "Play" to get audio.
> **CONTEXT:**
> The `onCanPlay` event strategy is completely unreliable for live WebM pipes. The browser is aborting the play request because the live buffer isn't ready when the event fires. We must programmatically simulate a hard "Stop" followed by a delayed "Play" to force the browser to destroy the old media state.
> **EXECUTION RULES:** > Use strict TDD. You are strictly forbidden from relying on `canplay` or `loadeddata` events to trigger playback for this phase.
> ---
> 
> 
> ### Phase 1: Programmatic "Stop-Then-Play" Sequence (Frontend)
> 
> 
> **TASK:** Modify the file switching logic in `src/contexts/PlaybackContext.tsx`:
> 1. **The Hard Stop:** When a new file is double-clicked, immediately execute the exact logic used by the transport's "Stop" button.
> * Call `audio.pause()`.
> * Remove the source entirely: `audio.removeAttribute('src')` or `audio.src = ''`.
> * Call `audio.load()` to flush the browser's internal demuxer.
> * Zero out the virtual playhead and React states.
> 
> 
> 2. **The Event Loop Yield:** Do NOT immediately set the new URL. You must yield to the Javascript event loop (e.g., using `setTimeout(() => { ... }, 50)`) to allow React and WaveSurfer to process the "Stopped" state and clear their internal caches.
> 3. **The Hard Play:** Inside that timeout, apply the new `/obr-stream` URL, call `audio.load()`, and immediately call `audio.play()`.
> 4. **Strict Error Logging:** Chain a `.catch(err => console.error("[Stream Swap Error]", err))` to the `.play()` promise so we can see exactly why the browser refuses to play if it fails again.
> 
> 
> **TEST:** Write a test (`scripts/test-hard-swap.ts`) that verifies the `setTimeout` and `removeAttribute` sequence is called when switching files.
> **CHECKPOINT:** Stop and evaluate. Do not proceed to Phase 2 until this sequence is implemented.
> ---
> 
> 
> ### Phase 2: State Enforcement & WaveSurfer Override (Frontend)
> 
> 
> **CONTEXT:** WaveSurfer might be intercepting the file change and issuing its own `pause()` command, overriding our play command.
> **TASK:** > 1. In `Transport.tsx` or `PlaybackContext.tsx`, ensure that any WaveSurfer `ready` or `load` events are NOT accidentally calling `.pause()` when the new stream connects.
> 2. Ensure the UI "Play/Pause" button strictly derives its visual state from `!audio.paused`, ensuring it never shows "Playing" while stuck at 0:00.
> **REPORTING:** Do not claim manual verification passed unless you have explicitly tested the UI. Output the exact sequence of console logs your code will produce during a track swap.

