status: ✅ Completed



**CRITICAL BUG REPORT: AmbiSwap Data Loss**

I just tested `AmbiSwap` (AmbiX -> FuMa) on a 3rd Order file.

* **Result:** The output file is **66% (2/3rds)** the size of the input.
* **Diagnosis:** You are failing to define the output bit depth. The input was **24-bit**, and FFmpeg defaulted the output to **16-bit**, causing massive data loss.

**Required Fix:**
Update `electron/handlers/ambiswap.ts` immediately.

1. **Force the Codec:** You must strictly define the audio codec. Since we are swapping formats for professional use, default to **24-bit PCM** (`-c:a pcm_s24le`) or **32-bit Float** (`-c:a pcm_f32le`) to prevent clipping during the gain normalization.
2. **Best Practice:** Ideally, inspect the input bit depth first and match it, but for now, **hardcode 24-bit (`pcm_s24le`)** as the safe minimum standard for this tool.

**Retest:**
Run the conversion again. The output file size must match the input file size (within a few kilobytes).

