

**Role:** Senior Full-Stack Architect (Electron)

**Status:**
**DIAGNOSIS:** `JSAmbisonics` is loaded (Fixed).
**NEW BUG:** The File Loading hangs.

* Log: `Requesting IPC Read: ...` (Frontend)
* Result: **Dead Silence.** No error, no success. The Promise never resolves.

**Hypothesis:**

1. **IPC Channel Mismatch:** Frontend calls `readFile` but Backend listens to `read-file`.
2. **IPC Buffer Limit:** Ambisonics files are huge. Sending 100MB+ over standard `ipcRenderer.invoke` often freezes or crashes Electron silently.

**Task:**
Debug and Fix the IPC Handler.

**Step 1: verify the Channel Name (The "Handshake")**

* **Frontend (`preload.ts`):** Check exactly what string is used in `ipcRenderer.invoke('THIS_STRING', path)`.
* **Backend (`electron/main.ts` or `handlers`):** Check exactly what string is used in `ipcMain.handle('THIS_STRING', ...)`.
* **Fix:** Ensure they match perfectly (e.g., use `read-file-buffer`).

**Step 2: Add Backend Logs (The "Eyes")**
We need to see if the Main process even *gets* the request.

* In your backend handler, add these logs:
```typescript
ipcMain.handle('read-file-buffer', async (event, filePath) => {
    console.log("MAIN: Received read request for:", filePath); // <--- VITAL
    try {
        const buffer = await fs.promises.readFile(filePath);
        console.log("MAIN: File read success. Size:", buffer.length);
        return buffer;
    } catch (e) {
        console.error("MAIN: Read failed:", e);
        throw e;
    }
});

```



**Step 3: The "Chunking" Fix (Crucial for Audio)**
If the file is >100MB, sending the whole buffer in one go often fails.

* **Temporary Test:** Try loading a **very small .wav file** (like a 2-second clip).
* **If small works but big fails:** You **MUST** switch to "Chunked Reading" or a Stream approach, but for now, let's just get *any* file to load.

**Action:**

1. Check the IPC channel names match.
2. Add the `console.log` in the Main process handler.
3. **Run the App from the Terminal** so you can see the **Main Process Logs** (these do not show up in the Chrome DevTools console).

**Report back:**
When you drop the file, do you see `MAIN: Received read request` in the terminal?

