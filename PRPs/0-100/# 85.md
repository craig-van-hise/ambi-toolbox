### Product Requirements Prompt (PRP): Execution Unblocker

> **OBJECTIVE:** Execute the testing and implementation plan autonomously. Do not pause for human permission between steps.
> **CONTEXT:**
> Your architectural analysis is correct. Target `src/contexts/PlaybackContext.tsx` instead of `Transport.tsx` for the HTML5 `Audio` integration. You have authorization to proceed.
> **EXECUTION CHAIN (Run these sequentially without pausing):**
> 1. **Test Backend:** Run `scripts/test-server.ts` in the background.
> 2. **Verify Stream:** Run `scripts/verify-stream-endpoint.js`. Assert that the WebM header signature (`1A 45 DF A3`) is successfully received.
> 3. **Implement Frontend:** Immediately upon test success, modify `src/contexts/PlaybackContext.tsx`.
> * Wire the existing HTML5 `Audio` instance to the `/obr-stream?file=<ENCODED_PATH>` endpoint.
> * Implement the required DOM event listeners (`stalled`, `waiting`, `playing`, `error`) to handle stream drops and reset the context state cleanly.
> 
> 
> 4. **Cleanup:** Kill the background test server.
> 
> 
> **REPORTING:** > Only report back to the user once ALL four steps are complete, the React codebase compiles, and the `PlaybackContext.tsx` has been successfully updated.
