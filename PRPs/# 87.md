### Product Requirements Prompt (PRP): Forcing the 3-Stage Pipeline (Phased TDD)

> **OBJECTIVE:** Eliminate the HTTP header crash and forcefully implement the 3-stage `obr_stream` pipeline in `ObrHandler.ts`.
> **EXECUTION RULES:** > You must execute this task using strict Test-Driven Development (TDD) across two distinct phases. You MUST write and execute the specified test at the end of each phase. You are strictly forbidden from proceeding to the next phase until the current phase's test yields a passing result (green light).
> ---
> 
> 
> ### Phase 1: Fix the HTTP Header Crash on Valid Requests
> 
> 
> **CONTEXT:** `ObrHandler.ts` is throwing `ERR_HTTP_HEADERS_SENT` when a *valid* file is requested. This indicates duplicate header writes (e.g., calling `res.writeHead` and then `.pipe(res)` trying to modify headers, or sending an error after headers are sent).
> **TASK:** Inspect the `/obr-stream` endpoint logic.
> 1. Ensure `res.writeHead(200, { 'Content-Type': 'audio/webm' })` is called exactly ONCE before any audio piping begins.
> 2. Ensure that if the stream fails later, it safely calls `res.end()` or `.destroy()` instead of trying to send a 500 status code after headers are already flushed.
> **TEST:** Write `scripts/test-header-crash.ts`. It must mock a valid request to `/obr-stream`, consume the stream for 2 seconds, abort the request, and assert that NO unhandled promise rejections or header errors are thrown in the server logs.
> **CHECKPOINT:** Stop and evaluate. Do not proceed to Phase 2 until this test passes.
> 
> 
> ---
> 
> 
> ### Phase 2: The Un-Bypassable 3-Stage Pipeline
> 
> 
> **CONTEXT:** You previously bypassed the C++ sidecar. You are currently mapping `pcm_s16le` directly to `opus`. This is unacceptable and breaks the spatial audio math. You MUST use the `obr_stream` executable.
> **TASK:** Gut the `spawn` logic in `ObrHandler.ts`. You must implement EXACTLY this 3-process architecture:
> 1. **Decoder Process:** >    `ffmpeg -i <FILE> -f f32le -ar 48000 -ac <CHANNELS> pipe:1`
> 2. **OBR Sidecar Process:** >    `./obr_stream <CHANNELS> <BINAURAL_FLAG>` *(reads raw f32le from stdin, writes stereo f32le to stdout)*
> 3. **Encoder Process:** >    `ffmpeg -f f32le -ar 48000 -ac 2 -i pipe:0 -c:a libopus -f webm pipe:1`
> 
> 
> **Piping Logic:**
> You must strictly chain them:
> `decoder.stdout.pipe(obr.stdin);`
> `obr.stdout.pipe(encoder.stdin);`
> `encoder.stdout.pipe(res);`
> **TEST:** Write `scripts/test-strict-pipeline.ts`. This test MUST read the source code of `ObrHandler.ts` (using `fs.readFileSync`) and assert that the string `'obr_stream'` and the string `decoder.stdout.pipe` exist in the file. Then, it must make a request to the endpoint and verify a valid WebM buffer is returned.
> **CHECKPOINT:** Report back to the user ONLY when Phase 2 is complete, both tests are green, and the 3 distinct processes are implemented.
