# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
This phase is a strict rollback and correction. The worker agent previously mangled `src/tools/AmbiRotate/index.tsx` by hallucinating three identical `<Knob>` components, destroying the original horizontal and vertical sliders. The human user does not have the commit hash. The agent must perform "Git archaeology" to autonomously find the last known good version of this file in the repository's history and restore it, before applying the safe 3-column layout constraints.

## 2. Technical Decisions & Dependencies
* **Automated Git Archaeology:** The agent will use `git show HEAD~<N>:path/to/file` to read previous versions of the file into memory without entering a detached HEAD state. 
* **Heuristic for the "Correct" File:** The correct legacy version of `src/tools/AmbiRotate/index.tsx` will have distinct UI implementations for Yaw, Pitch, and Roll. It will *not* contain three identical `<Knob />` instances. 
* **Hands-Off DOM Policy:** Once the authentic file is restored, the agent may only edit the outermost wrapper `div`s to ensure they fit the `minmax(250px, 1fr)` center column. It must not touch the restored slider controls.

## 3. Implementation Phases

### Phase 1: Autonomous Git Search & Restore — ✅ Completed
* **Objective:** Find the authentic AmbiRotate UI in the Git history and restore it to the working directory.
* **Instructions:**
  1. **SEARCH:** Use the terminal to inspect older versions of the file. Run `git show HEAD~3:src/tools/AmbiRotate/index.tsx`. 
  2. **EVALUATE:** Read the output. Look at the JSX rendering the Yaw and Pitch controls. If they are just `<Knob />` elements, you haven't gone back far enough.
  3. **ITERATE:** If incorrect, run `git show HEAD~5...`, then `HEAD~10...`, etc., until you find the file state where Yaw and Pitch are implemented as their original distinct sliders.
  4. **RESTORE:** Once you find the correct `HEAD~N` reference, overwrite the current working file with that exact content: `git show HEAD~N:src/tools/AmbiRotate/index.tsx > src/tools/AmbiRotate/index.tsx`.
  5. **CLEANUP:** Do the exact same search and restore process for `src/tools/AmbiRotate/components/Knob.tsx` to ensure it is returned to its pure radial SVG state without hallucinated variants.
* **TDD Checkpoint:**
  * **Test Case 1:** [Given the restored `index.tsx`, When parsing the JSX, Then assert that Yaw and Pitch do NOT use the `<Knob>` component].
  * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Execute the search and restore. Do not proceed until the authentic distinct sliders are back in the file. Once verified, set status to ✅ Completed.

### Phase 2: Careful Layout Integration — ✅ Completed
* **Objective:** Adapt the restored, authentic UI to the 3-column layout without causing flex-wrap jumps or mangling the controls again.
* **Instructions:**
  1. Open the newly restored `src/tools/AmbiRotate/index.tsx`.
  2. Locate the parent container that wraps the Yaw, Pitch, and Roll control cards.
  3. Apply `flex flex-wrap justify-center gap-4` to this parent wrapper so it flows correctly when the viewport shrinks.
  4. Apply a fixed width utility (e.g., `w-[140px]`) and `shrink-0` to the outermost `div` of the Yaw, Pitch, and Roll cards individually.
  5. **CRITICAL:** Do NOT modify the inner sliders, inputs, event handlers, or state bindings. Remove any vestigial Transport timeline/buttons if they were brought back with the older file version.
* **TDD Checkpoint:**
  * **Test Case 1:** [Given the `AmbiRotate` UI, When inspecting the cards, Then assert the wrappers possess a fixed width class and `shrink-0`].
  * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Apply layout wrappers carefully. Once PASS, set status to ✅ Completed.

## 4. Final Review & Validation
* **Technical Audit:** Verify the file history was successfully pulled and no hallucinated generic inputs or repeated knobs exist.
* **Status Reconciliation:** Verify Phases 1 and 2 are marked ✅.
* **Final Human Sign-off:** Present the build to the user to confirm the authentic horizontal and vertical sliders have returned and successfully survive window resizing.