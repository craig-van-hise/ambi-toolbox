status: ✅ Completed



### Product Requirements Prompt (PRP): Scrubber Auto-Resume Wiring


> **CRITICAL BUG FIX: SCRUBBER FAILS TO AUTO-RESUME**
> The backend is successfully generating streams at the new `start` offsets. However, the frontend is failing to resume playback after a seek. The native `<audio>` element pauses when its `src` changes, and the timeline seek handler is currently failing to utilize the `playIntentRef` to automatically restart it.
> **EXECUTION RULES:**
> Do NOT alter the backend or the 3-Step Linear Pipeline. Only modify the timeline interaction handlers.
> ---
> 
> 
> ### Phase 1: Capture State on Seek (Frontend)
> 
> 
> **CONTEXT:** When the user scrubs the timeline, we must remember if the track was playing *before* the URL changes.
> **TASK:** In `src/contexts/PlaybackContext.tsx` or `Transport.tsx` (wherever the seek/scrubber event is handled):
> 1. In the function that handles the seek interaction (e.g., `handleSeek`), check the current audio state *before* updating the offset state: `const wasPlaying = !audio.paused;`
> 2. Set the existing intent ref: `playIntentRef.current = wasPlaying;`
> 3. Then, update the `start` offset state (which triggers the URL regeneration).
> 
> 
> **TEST:** Add a temporary `console.log("Seek initiated. Was playing:", wasPlaying)` to the seek handler. Scrub the timeline while audio is playing and verify it logs `true`.
> ---
> 
> 
> ### Phase 2: Ensure Commit Hook Respects Intent on Seek (Frontend)
> 
> 
> **CONTEXT:** The Step 3 "Commit" `useEffect` from the previous refactor updates the `audio.src` whenever the URL changes (which includes when the `start` parameter changes).
> **TASK:** Verify that the Step 3 `useEffect` in `PlaybackContext.tsx`:
> 1. Correctly triggers when the `start` offset dependency changes (not just when the file changes).
> 2. Calls `audio.load()`.
> 3. Checks `if (playIntentRef.current) { audio.play().catch(...); playIntentRef.current = false; }`.
> *(Note: If the Step 3 hook already looks like this, Phase 2 requires no changes. Just ensure the dependency array includes the `start` state).*
> 
> 
> **REPORTING:** Report back when scrubbing the timeline while playing automatically resumes playback.
