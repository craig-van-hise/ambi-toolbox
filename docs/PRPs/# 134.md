# Product Requirements Prompt (PRP) — � Active

## 1. Project Context & Objectives
This execution phase addresses two critical failures from the previous sprint. First, the application's minimum window size is currently too large for a utility application; the center column must be allowed to shrink further. Second, the agent previously hallucinated a generic `<input type="range">` UI for `AmbiRotate`, overwriting the authentic custom components. Because the current `src/tools/AmbiRotate/index.tsx` state is corrupted, the agent MUST use `git` to restore the original file structure before migrating it to the new 3-column layout.

## 2. Technical Decisions & Dependencies
* **Relaxed Native Constraints:** In `electron/main.ts`, the `BrowserWindow` minimum dimensions must be reduced to `800x500`.
* **Aggressive Center Column Shrinkage:** The React CSS Grid definition in the main layout wrapper must reduce its center column `minmax` value to `minmax(250px, 1fr)`.
* **AmbiRotate Git Restoration:** The agent is strictly forbidden from manually rewriting the AmbiRotate UI. It must use `git log` and `git checkout` to restore `src/tools/AmbiRotate/index.tsx` to its state prior to the recent layout migration, ensuring the original `<Knob />` wiring is perfectly intact.

## 3. Implementation Phases

### Phase 1: Relaxing Global Layout & Window Constraints — ✅ Completed
* **Objective:** Allow the application window to scale down to a smaller, utility-friendly footprint.
* **Instructions:**
  1. Open `electron/main.ts`. Locate the `BrowserWindow` configuration.
  2. Change `minWidth` to `800` and `minHeight` to `500`.
  3. Open the top-level layout component (`src/App.tsx` or `src/components/ToolViews.tsx`).
  4. Locate the Tailwind grid class (e.g., `grid-cols-[250px_minmax(450px,_1fr)_320px]`).
  5. Update the center column's `minmax` to `250px` (e.g., `grid-cols-[250px_minmax(250px,_1fr)_300px]`).
  6. Ensure the Right Sidebar container retains a fixed width (e.g., `w-[300px] shrink-0`) so it does not collapse.
* **TDD Checkpoint:**
  * **Test Case 1:** [Given the Electron main process, When `createWindow` is called, Then assert the `minWidth` property is exactly 800].
  * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Apply the updated constraints. Once PASS, set status to ✅ Completed.

### Phase 2: AmbiRotate Git Restoration & Integration — ✅ Completed
* **Objective:** Eradicate the hallucinated AmbiRotate UI by extracting the authentic code from the Git history.
* **Instructions:**
  1. **EXECUTE TERMINAL COMMAND:** The agent must run `git log --oneline src/tools/AmbiRotate/index.tsx` to find the commit hash *before* the hallucinated generic UI was introduced.
  2. **RESTORE FILE:** Run `git checkout <TARGET_COMMIT_HASH> -- src/tools/AmbiRotate/index.tsx` to restore the authentic file. 
  3. **VERIFY:** Open the newly restored `src/tools/AmbiRotate/index.tsx`. Verify that it imports and uses the custom `<Knob />` component (Yaw, Pitch, Roll).
  4. **ADAPT TO NEW LAYOUT:** Carefully wrap the restored authentic code so it fits into the new Top-Center container space required by the 3-column layout. **Do not alter the `<Knob />` props or internal state wiring.**
* **TDD Checkpoint:**
  * **Test Case 1:** [Given the `AmbiRotate` tool view, When rendered, Then assert that ZERO `<input type="range">` elements exist in the DOM].
  * **Test Case 2:** [Given the `AmbiRotate` tool view, When rendered, Then assert the custom `Knob` components are mounted with their original state bindings].
  * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Execute the Git restoration commands, adapt the layout wrapper, and verify. Once verified, set status to ✅ Completed.
  * **HUMAN VALIDATION REQUIRED:** STOP. Ask the user to visually confirm that the AmbiRotate dials look exactly like the original custom dials and that moving them updates the correct rotation state.

## 4. Final Review & Validation
* **Technical Audit:** Verify that `electron/main.ts` correctly applies the `800x500` limits, and ensure `src/tools/AmbiRotate/index.tsx` has successfully been restored via Git.
* **Status Reconciliation:** Verify Phases 1 and 2 are marked ✅.
* **Final Human Sign-off:** Present the finalized build to the user. Instruct the user to "Resize the window vertically and horizontally to confirm the tighter footprint, and verify the AmbiRotate dials are completely restored."