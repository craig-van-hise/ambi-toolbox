It is incredibly frustrating when an agent "fixes" a UI layout but silently corrupts the underlying data by filling dropdowns with hallucinated variables. This is a classic symptom of context-window drift: the agent forgot the exact HRTF filenames and bitrate standards, so it confidently invented fake ones to make the UI compile.

We are going to lock this down by extracting these configuration values into hardcoded constants, forcing the components to read from a single source of truth, and mapping the HRTF list directly to the actual files existing in your repository.

Here is the highly deterministic Product Requirements Prompt (PRP) to force the agent to correct these data regressions.

```markdown
# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives
This execution phase resolves critical data integrity regressions introduced by recent UI refactoring. The worker agent previously hallucinated values for the dropdown menus in the Ambisonic rendering and encoding tools. Specifically, `Ambix2Bin` is referencing non-existent `.sofa` files, causing backend crashes, and the bitrate selectors for `Ambix2Opus`, `Ambix2Ogg`, and `Ambix2IAMF` are displaying incorrect, non-standardized totals instead of the required "Per Channel" metrics. 

The objective is to audit the file system for the true HRTF assets, implement a standardized bitrate configuration object, and strictly map the UI components to these ground-truth values.

## 2. Technical Decisions & Dependencies
* **Filesystem Ground Truth (HRTF):** The agent must strictly audit `assets/hrtf/` and hardcode the dropdown in `Ambix2BinView.tsx` to exactly match the filenames present (e.g., `MIT_KEMAR_Normal.sofa`, `Neumann_KU100_48k.sofa`). Hallucinated options must be purged.
* **Bitrate Standardization:** A unified configuration constant must be applied across `Ambix2OpusView.tsx`, `Ambix2OggView.tsx`, and (if applicable) `Ambix2IAMFView.tsx`. The options must strictly be: Low (32kbps), Medium (64kbps), High (96kbps), Ultra (128kbps), and Technical Max (256kbps). 
* **UI Label Nomenclature:** The `<label>` element for all bitrate dropdowns must be reverted to the exact string: `TARGET BITRATE (PER CHANNEL)`.

## 3. Implementation Phases

### Phase 1: Ambix2Bin HRTF Data Hardening — ✅ Completed
* **Objective:** Purge hallucinated `.sofa` files from the UI state and map exclusively to verified local assets.
* **Instructions:**
  1. **AUDIT:** Use terminal commands (e.g., `ls assets/hrtf/`) to list the exact `.sofa` files present in the repository.
  2. Open `src/components/tools/Ambix2BinView.tsx` (or the `constants.ts` file if the dropdown list is managed globally).
  3. Locate the array or JSX defining the HRTF `<select>` options.
  4. **DELETE** all hallucinated entries.
  5. **INJECT** exact string matches for the `.sofa` files discovered in Step 1. Ensure the `value` prop maps to the exact filename the backend requires.
* **TDD Checkpoint:**
  * **Test Case 1:** [Given the `Ambix2BinView` component, When rendered, Then assert the HRTF `<select>` element contains exactly the same number of options as there are `.sofa` files in the `assets/hrtf` directory].
  * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Audit the directory and correct the select options. Once PASS, set status to ✅ Completed.

### Phase 2: Global Bitrate Standardization — ✅ Completed
* **Objective:** Implement a universal, standardized "Per Channel" bitrate configuration across all applicable encoding tools.
* **Instructions:**
  1. Define a shared constant (e.g., in `src/constants.ts` or instantiated directly in the target views) containing the following exact structure:
     ```typescript
     export const BITRATE_OPTIONS = [
       { label: 'Low (32kbps)', value: 32000 },
       { label: 'Medium (64kbps)', value: 64000 },
       { label: 'High (96kbps)', value: 96000 },
       { label: 'Ultra (128kbps)', value: 128000 },
       { label: 'Technical Max (256kbps)', value: 256000 }
     ];
     ```
  2. Open `src/components/tools/Ambix2OpusView.tsx`, `src/components/tools/Ambix2OggView.tsx`, and `Ambix2IAMFView.tsx`.
  3. Replace their current, hallucinated `<select>` option lists with a `.map()` over `BITRATE_OPTIONS`.
  4. Ensure the UI text node labeling this dropdown is strictly updated to: `TARGET BITRATE (PER CHANNEL)`. Remove any styling that forces it to read "OUTPUT BITRATE".
* **TDD Checkpoint:**
  * **Test Case 1:** [Given the `Ambix2OpusView`, When parsed, Then assert the bitrate `<select>` contains exactly 5 options matching the `BITRATE_OPTIONS` constant].
  * **Test Case 2:** [Given the `Ambix2OggView`, When parsed, Then assert a text element exists with the exact string "TARGET BITRATE (PER CHANNEL)"].
  * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Implement the shared constant and update the tool views. Once verified, set status to ✅ Completed.

## 4. Final Review & Validation
* **Technical Audit:** Verify that no rogue string literals representing fake `.sofa` files exist anywhere in the `src/` directory, and that `Ambix2Opus`, `Ambix2Ogg`, and `Ambix2IAMF` share the exact same bitrate logic.
* **Status Reconciliation:** Verify Phases 1 and 2 are marked ✅.
* **Final Human Sign-off:** Present the build to the user. Instruct the user to "Open Ambix2Bin and verify the HRTF dropdown only shows your real SOFA files, then open Ambix2Opus to verify the bitrate dropdown matches the requested 32-256kbps 'Per Channel' standard."

```