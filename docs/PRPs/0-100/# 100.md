status: ✅ Completed



### Product Requirements Prompt (PRP): Freeze the Intent Flag


> **CRITICAL BUG FIX: REACT STATE THRASHING ERASING PLAY INTENT**
> The application is successfully fetching the new stream, but it fails to auto-play on subsequent track switches. The logs reveal that the `shouldPlay` intent state is being overwritten to `false` by React re-renders *while* the asynchronous `WavDecoder` is running. By the time Step 3 executes, the intent to play has been erased.
> **EXECUTION RULES:**
> Do NOT refactor the linear pipeline. Only change how the "play intent" is stored.
> ---
> 
> 
> ### Phase 1: Convert Intent to a Mutable Ref (Frontend)
> 
> 
> **CONTEXT:** We need a variable that survives async operations and component re-renders without triggering new renders itself.
> **TASK:** In `src/contexts/PlaybackContext.tsx`:
> 1. Replace the `shouldPlay` (or `playbackIntent`) `useState` variable with a `useRef`: `const playIntentRef = useRef(false);`
> 2. In the `onDoubleClick` handler (Step 1), set it directly: `playIntentRef.current = true;`
> 3. In the stream commit hook (Step 3), after calling `audio.load()`, read the ref:
> `if (playIntentRef.current) { audio.play().catch(...); playIntentRef.current = false; }`
> 
> 
> **TEST:** Run the Dev Environment.
> * Double-click Track A. (Wait for it to play).
> * Double-click Track B.
> * **Pass Criteria:** The audio seamlessly transitions to Track B and begins playing automatically because `playIntentRef.current` survived the probe delay.
> 
> 
> **REPORTING:** Report back only when double-clicking multiple different files successfully auto-plays every single time.

