status: ✅ Completed


### Product Requirements Prompt (PRP): Frontend Stream Integration (`Transport.tsx`)

> **OBJECTIVE:** > Update the existing React transport component (`src/components/Transport.tsx`) to consume the newly verified `/obr-stream` backend endpoint using its existing WaveSurfer.js instance in MediaElement mode. Do NOT create a new transport component.
> **CONTEXT:**
> The backend 3-stage pipeline (FFmpeg Decode -> C++ `obr_stream` Sidecar -> FFmpeg Encode) is complete. A local server is now listening and exposing a live WebM/Opus stream via the `/obr-stream` HTTP endpoint. AmbiToolbox is a closed-source commercial application.
> **TECHNICAL REQUIREMENTS:**
> 1. **Target File:** All UI modifications must be strictly contained within `src/components/Transport.tsx`.
> 2. **MediaElement Integration:** Locate the existing WaveSurfer initialization logic in `Transport.tsx`. Ensure it is configured for `MediaElement` mode. Instantiate a standard HTML5 `Audio` object, set its `src` attribute to the local backend `/obr-stream` endpoint, and pass this `Audio` object as the `media` property to WaveSurfer.
> 3. **Query Parameter Injection:** Update the logic in `Transport.tsx` so that when a user selects an audio file, its absolute path is safely URL-encoded and passed to the stream endpoint (e.g., `http://localhost:<PORT>/obr-stream?file=<ENCODED_PATH>`).
> 4. **Buffer & Error Handling:** Attach native DOM event listeners (`stalled`, `waiting`, `playing`, `error`) to the HTML5 `Audio` object. If the backend stream drops or FFmpeg fails, the component must catch the error, log it, and cleanly reset the transport state (e.g., pause playback, reset UI).
> 
> 
> **THE "GOD RULES" (MANDATORY CONSTRAINTS):**
> * **Strict TDD / Automated Verification:** You MUST NOT ask the user to test the UI. Before touching `Transport.tsx`, write a lightweight Node script (`scripts/verify-stream-endpoint.js`) that performs an HTTP `GET` request to `/obr-stream` with a test file, reads the first 100 bytes of the response, and asserts the presence of the WebM header signature (`1A 45 DF A3`). Only proceed to edit `Transport.tsx` once this network test passes.
> * **No Hallucinated Math/DSP:** The frontend's ONLY job is playback and UI transport control (Play, Pause, Seek). Do not introduce any Web Audio API spatialization, gain nodes, or panners into `Transport.tsx`. The C++ sidecar handles 100% of the math.
> * **Commercial Safety:** Rely strictly on native HTML5 APIs and the existing WaveSurfer dependency. Do not install any new React audio wrappers.
> 
> 
> **EXECUTION:**
> 1. Write and execute the HTTP stream verification script (`scripts/verify-stream-endpoint.js`).
> 2. Modify `src/components/Transport.tsx` to wire up the MediaElement stream.
> 3. Report back ONLY when the verification test passes and the React codebase compiles without errors.
