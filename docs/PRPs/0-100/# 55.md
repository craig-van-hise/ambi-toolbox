status: ✅ Completed



# Product Requirements Prompt: UI Layout Emergency Fix (AmbiTrim)

**Priority:** CRITICAL
**Context:** The current implementation is visually broken. It renders TWO drop zones simultaneously (one in the workspace, one in the footer). This violates the design system.
**Goal:** Force a clean separation between the **Dynamic Workspace** (Top) and the **Static Control Deck** (Bottom).

## 1. The Layout Architecture (The "Split View")
status: ✅ Completed

You must implement the following container structure. Do not deviate.

```tsx
// The Main Container (Fills the right side of the screen)
<div className="flex flex-col h-screen bg-slate-900 text-white">

  {/* SECTION A: Header (Static) */}
  <div className="p-8 pb-4">
    <h1 className="text-3xl font-bold text-purple-400">AmbiTrim</h1>
    <p className="text-gray-400 mt-2">Lossless trimming for Ambisonic master files.</p>
  </div>

  {/* SECTION B: The Workspace (Dynamic - Flex Grow) */}
  {/* This is the ONLY place a Drop Zone can ever exist. */}
  <div className="flex-1 relative min-h-0 w-full flex flex-col">
    {/* LOGIC: SWAP VIEWS BASED ON FILE STATE */}
    { !file ? (
        // STATE 1: No File Loaded
        // Render the Standard DropZone here. 
        // It must fill this entire section.
        <StandardDropZone />
    ) : (
        // STATE 2: File Loaded
        // The DropZone is REMOVED. The Waveform takes its place.
        <WaveformVisualizer />
    )}
  </div>

  {/* SECTION C: The Control Deck (Static Footer) */}
  {/* RESTRICTION: NEVER put a Drop Zone here. Inputs & Buttons ONLY. */}
  <div className="border-t border-gray-700 bg-slate-800 p-6 z-10">
    
    {/* Row 1: Time Inputs */}
    <div className="grid grid-cols-2 gap-4 mb-6">
      <TimeInput label="START TIME" value={startTime} />
      <TimeInput label="END TIME" value={endTime} />
    </div>

    {/* Row 2: Main Action */}
    <button className="w-full py-4 bg-purple-600 hover:bg-purple-500 rounded-md font-bold text-white text-lg transition-all">
      Export Trimmed File
    </button>
  </div>

</div>

```

## 2. Explicit Prohibitions (Do Not Do This)
status: ✅ Completed

1. **NO Footer Drop Zones:** Look at the footer (Section C). It contains Inputs and a Button. You are **FORBIDDEN** from placing a dashed-border `<div>`, a `<DropZone>`, or any "Drag file here" text inside Section C.
2. **NO Double Rendering:** You must never show the Drop Zone and the Waveform at the same time. They are mutually exclusive.
3. **NO "Scissors" Icon Drop Zone:** The screenshot showed a custom "Scissors" drop zone. Delete it. Use the standard Drop Zone component used by `Ambix2Opus` (the one with the "Upload/Up Arrow" icon).

## 3. Human Verification (HITL)
status: ✅ Completed

**Instructions for the Agent:**
"Refactor the code to match the structure above exactly. Remove the 'Scissors' drop zone entirely."

**Test Procedure (User):**

1. **Launch App:** Open AmbiTrim.
2. **Top-Down Scan:**
* **Header:** Title and text.
* **Middle:** ONE single Drop Zone (Up Arrow Icon).
* **Bottom:** A grey panel containing **only** two time inputs and one purple button.


3. **Bug Check:** Verify there is no dashed box inside the grey bottom panel.

**Go.**