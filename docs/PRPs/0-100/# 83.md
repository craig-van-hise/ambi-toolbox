status: ✅ Completed


# Course Correction: Fetch the OBR Source

## 1. Do NOT Use Mocks
status: ✅ Completed

Do not proceed with a mock or placeholder interface. We need the actual Google OBR C++ source code to compile the sidecar.

## 2. Action: Fetch the Repository
status: ✅ Completed

The `src/cpp/vendor/obr` directory is currently incomplete (likely an uninitialized submodule or empty artifact).

1. Delete the current `src/cpp/vendor/obr` directory to clear out the hollow artifact.
2. Run the following command to clone the official repository directly into that location:
`git clone https://github.com/google/obr.git src/cpp/vendor/obr`

## 3. Verification & Resumption
status: ✅ Completed

1. Verify that the core library files now exist (e.g., check that `src/cpp/vendor/obr/obr/obr_impl.h` and `src/cpp/vendor/obr/obr/audio_buffer.h` are present).
2. Once the source files are confirmed, **resume executing the previous PRP (OBR Sidecar Integration)** starting from Phase 1 (writing `obr_stream.cc` and `CMakeLists.txt`).
3. Remember: We are using CMake to build this, so do NOT attempt to run Bazel commands inside the cloned repository.

