
# Product Requirements Prompt (PRP) — ✅ Completed

## 1. Project Context & Objectives

This phase is a strict restoration of the `AmbiTrim` tool. The current version is a "hallucinated" regression that lacks the original `WaveSurfer.js` logic and triggers a fatal `FFmpeg code 8` error because it has lost the correct proxy-generation parameters. The agent must autonomously search the Git history to find the last known version of `src/components/tools/AmbiTrim.tsx` where the waveform worked, restore it, and then apply the necessary 3-column layout wrappers.

## 2. Technical Decisions & Dependencies

* **Git Archaeology:** The agent must use `git show` to inspect versions of the file from 1–3 weeks ago.
* **Heuristic for "Good" Version:** The correct version will contain `import WaveSurfer from 'wavesurfer.js'` and an IPC call to `trim:generateProxy` that correctly maps the 0th-order (W) channel.
* **Strict UI Restoration:** Once the working logic is found, the agent is **strictly forbidden** from rewriting the waveform logic. It may only remove the local `DropZone` and wrap the existing UI in the new Top-Center flex container.

## 3. Implementation Phases

### Phase 1: Autonomous Git Search & Restoration — ✅ Completed

* **Objective:** Find the authentic, working `AmbiTrim` UI in the Git history.
* **Instructions:**
1. **SEARCH:** Run `git log --oneline --graph --all -- src/components/tools/AmbiTrim.tsx` to see the history.
2. **INSPECT:** Use `git show HEAD~10:src/components/tools/AmbiTrim.tsx` (and increment/decrement as needed) to read the code.
3. **IDENTIFY:** Look for the version that includes the `WaveSurfer` initialization and the specific `useEffect` that triggers `trim:generateProxy`.
4. **RESTORE:** Overwrite the current file with the identified working version: `git show <COMMIT_HASH>:src/components/tools/AmbiTrim.tsx > src/components/tools/AmbiTrim.tsx`.


* **TDD Checkpoint:**
* **Test Case 1**: [Given the restored `AmbiTrim.tsx`, When parsing the file, Then assert it contains `import WaveSurfer` and no generic hallucinated DropZone markup].
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Perform the archaeology. Do not stop until you have the version with the working waveform logic. Once restored, set status to ✅ Completed.



### Phase 2: Context Wiring & Layout Cleanup — ✅ Completed

* **Objective:** Connect the restored waveform to the global file queue and remove the redundant local DropZone.
* **Instructions:**
1. Open the restored `src/components/tools/AmbiTrim.tsx`.
2. **DELETE** the internal `DropZone` component and any "No File Selected" placeholders that were part of that older version's center-screen UI.
3. **WIRE:** Ensure the component now pulls the `activeFile` from the global `useFileQueue` hook.
4. **TRIGGER:** The `useEffect` that calls `trim:generateProxy` must now trigger automatically whenever a user selects a file from the **Right Sidebar**.


* **TDD Checkpoint:**
* **Test Case 1**: [Given `AmbiTrim` is active, When a file is selected in the Right Sidebar, Then assert `trim:generateProxy` is invoked with the file's absolute path].
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Wire the global state. Once verified, set status to ✅ Completed.



### Phase 3: Vertical Stacking & Action Sync — ✅ Completed

* **Objective:** Fit the waveform and time inputs into the 3-column architecture.
* **Instructions:**
1. Wrap the restored WaveSurfer container in a `div` with `flex-1 min-h-[250px]`.
2. Apply `flex flex-col gap-6` to ensure the waveform, zoom slider, and start/end time inputs stack vertically without overlapping.
3. **DELETE** the local "Export Trimmed File" button.
4. Ensure the tool's `actionLabel` in `src/constants.ts` is set to `"Export Trimmed File"` so the global Bottom-Center button handles the execution.


* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Finalize the styling. Once PASS, set status to ✅ Completed.

## 4. Final Review & Validation

* **Technical Audit:** Verify the `FFmpeg code 8` error is gone by confirming the backend is receiving the correct 0th-order mapping parameters from the restored frontend logic.
* **Status Reconciliation:** Verify Phases 1–3 are marked ✅.
* **Final Human Sign-off:** "Select AmbiTrim, click a file in the sidebar, and confirm the original WaveSurfer waveform appears immediately with the correct zoom and time markers."

